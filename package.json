{
  "name": "rotate",
  "version": "0.0.0",
  "description": "Rotate an input",
  "scripts": {
    "build": "tsc",
    "test:watch": "nodemon -e ts,md -w src -w test -x \"npm run test --silent\"",
    "pretest": "rm -rf out && mkdir out",
    "test": "ts-node ./src/main.ts"
  },
  "author": "Cole Lawrence",
  "license": "ISC",
  "devDependencies": {
    "mocha": "^3.1.2",
    "nodemon": "^1.11.0",
    "ts-node": "^1.6.1",
    "typescript": "^2.1.0-dev.20161105"
  },
  "dependencies": {
    "@types/mocha": "^2.2.32",
    "@types/node": "^6.0.46",
    "typescript-collections": "^1.2.3"
  }
}
